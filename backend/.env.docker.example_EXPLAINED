# ============================================
# PENJELASAN: .env.docker.example - UNTUK DOCKER
# ============================================
# Fungsi: Template env saat backend jalan di dalam container Docker. Copy jadi .env.docker.
#         Bedanya dengan .env.example: DB_HOST=mysql (nama service di docker-compose), bukan localhost.
# Alur: docker-compose baca env_file: ./backend/.env.docker; backend di container pakai nilai ini.
# Analogi: Daftar pengaturan untuk "gedung Docker"; alamat database = nama teman di gedung yang sama (mysql), bukan "rumah sendiri" (localhost).
# ============================================

# ============================================
# ENV UNTUK DOCKER (BACKEND DI CONTAINER)
# ============================================
# Copy jadi .env.docker:  cp .env.docker.example .env.docker
# Jangan commit .env.docker ke GitHub!
#
# Dipakai HANYA saat jalan "docker compose up". Backend di container
# butuh DB_HOST=mysql (nama service), bukan localhost.

PORT=5000
NODE_ENV=development

DB_HOST=mysql
DB_USER=root
DB_PASSWORD=rootpassword
DB_NAME=lostfound_db

JWT_SECRET=secret_key_lostfound_2024_change_this_in_production
UPLOAD_DIR=uploads
FRONTEND_URL=http://localhost:3000

# RINGKASAN: Template env Docker. DB_HOST=mysql agar backend di container bisa konek ke container MySQL.
# KESALAHAN PEMULA: Pakai DB_HOST=localhost di Docker (backend tidak bisa konek; localhost = di dalam container sendiri).
# ============================================
