# ============================================
# PENJELASAN: .env.example (backend) - UNTUK JALAN LOKAL (NON-DOCKER)
# ============================================
# Fungsi: Template variabel lingkungan. Copy jadi .env (cp .env.example .env), lalu isi nilai sesuai laptop/XAMPP.
#         Dipakai saat backend jalan di komputer kamu; MySQL jalan di localhost. Untuk Docker pakai .env.docker.example.
# Alur: Backend (dotenv) baca file .env → process.env.PORT, process.env.DB_HOST, dll terisi.
# Analogi: Daftar "kosong" yang harus kamu isi (password DB, secret JWT); aslinya pakai file .env (jangan di-commit).
# ============================================

# ============================================
# ENVIRONMENT VARIABLES - UNTUK JALAN LOKAL (NON-DOCKER)
# ============================================
# Copy jadi .env:  cp .env.example .env
# Jangan commit .env ke GitHub!
#
# Dipakai saat backend jalan di laptop (XAMPP/MySQL di host).
# Untuk Docker pakai .env.docker.example → .env.docker (lihat DOCKER_PANDUAN.md).

# Server Configuration
PORT=5000
NODE_ENV=development

# Database (MySQL di laptop / XAMPP)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=lostfound_db

# JWT Secret Key (ganti dengan string random yang aman)
JWT_SECRET=secret_key_lostfound_2024_change_this_in_production

# Upload & CORS
UPLOAD_DIR=uploads
FRONTEND_URL=http://localhost:3000

# RINGKASAN: Template env lokal. Copy ke .env, isi DB_PASSWORD dll. ISTILAH: .env = file variabel lingkungan (rahasia).
# KESALAHAN PEMULA: Commit .env ke Git (bahaya); lupa copy .env sehingga process.env kosong; salah DB_HOST saat pakai Docker (harus mysql).
