# ============================================
# ENVIRONMENT VARIABLES - TEMPLATE (SATU UNTUK SEMUA)
# ============================================
# Copy file ini menjadi .env:  cp .env.example .env
# Jangan commit file .env ke GitHub!
#
# - Jalan lokal (XAMPP/MySQL di laptop): pakai isi di bawah apa adanya.
# - Jalan pakai Docker: copy yang sama ke .env; docker compose akan
#   set DB_HOST=mysql & password otomatis (override), jadi tidak perlu ubah manual.

# Server Configuration
PORT=5000
NODE_ENV=development

# Database (lokal: pakai ini; Docker: di-override oleh docker-compose)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=lostfound_db

# JWT Secret Key (ganti dengan string random yang aman)
JWT_SECRET=secret_key_lostfound_2024_change_this_in_production

# Upload & CORS
UPLOAD_DIR=uploads
FRONTEND_URL=http://localhost:3000
