# ============================================
# PRODUCTION ENVIRONMENT VARIABLES (REFERENSI)
# ============================================
# Letak: di ROOT project (bukan di backend/) — soalnya dipakai saat DEPLOY
#        ke Vercel/Railway; env production di-set di dashboard hosting,
#        bukan dari file .env di repo.
#
# BUKAN untuk di-copy ke backend/.env !
# Ini cuma DAFTAR REFERENSI: variabel apa saja yang harus kamu SET
# di dashboard hosting production (Vercel → Settings → Environment Variables,
# Railway, Render, dll.) supaya backend yang deploy (api/index.js) bisa
# connect DB & CORS benar.
# ============================================

# Server Configuration
NODE_ENV=production

# Database Configuration (Misal: PlanetScale / Aiven / Railway)
DB_HOST=your-production-db-host
DB_USER=your-production-db-user
DB_PASSWORD=your-production-db-password
DB_NAME=lostfound_db
DB_PORT=3306

# JWT Secret Key (GUNAKAN STRING RANDOM PANJANG!)
# Generate di terminal: openssl rand -base64 32
JWT_SECRET=

# Frontend URL (CORS)
FRONTEND_URL=https://your-vercel-app.vercel.app

# Upload Directory (Vercel butuh external storage seperti S3/Cloudinary)
# Untuk sementara di Vercel, file upload tidak persistent jika pakai local fs
UPLOAD_DIR=/tmp
