# ============================================
# PRODUCTION ENVIRONMENT VARIABLES
# ============================================

# Server Configuration
NODE_ENV=production

# Database Configuration (Misal: PlanetScale / Aiven / Railway)
DB_HOST=your-production-db-host
DB_USER=your-production-db-user
DB_PASSWORD=your-production-db-password
DB_NAME=lostfound_db
DB_PORT=3306

# JWT Secret Key (GUNAKAN STRING RANDOM PANJANG!)
# Generate di terminal: openssl rand -base64 32
JWT_SECRET=

# Frontend URL (CORS)
FRONTEND_URL=https://your-vercel-app.vercel.app

# Upload Directory (Vercel butuh external storage seperti S3/Cloudinary)
# Untuk sementara di Vercel, file upload tidak persistent jika pakai local fs
UPLOAD_DIR=/tmp
